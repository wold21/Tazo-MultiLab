// import { useEffect, useState } from 'react';
// import { BlockLayer } from '../../component/blockLayer/blockLayer';
import { Wrapper, MainGameBox } from './aimAbilityStyle';

export const AimAbility = () => {
    return (
        <Wrapper>
            <MainGameBox></MainGameBox>
        </Wrapper>
    );
};
